[account]
email = your@email.com
password = yourpassword

# Имя сессии для этой машины. Должно совпадать с именем файла в
# session_imports/<name>.xlsx. На первом запуске сессия импортируется,
# дальше warmup гоняется на ней же. Если оставить пустым — импорт и
# поиск по имени пропускаются (старое поведение).
[session]
name = Jane Morrison
